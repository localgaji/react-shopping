apiVersion: v1
kind: ConfigMap
metadata:
  name: frontconfig
data:
  nginx.conf: |-
    server {
      location / {
        try_files $uri $uri/ /index.html;
        if (!-e $request_filename){
          rewrite ^(.*)$ /index.html break;
        }
      }
    }
