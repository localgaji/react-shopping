apiVersion: v1
kind: ConfigMap
metadata:
  name: frontconfig
data:
  nginx.conf: |-
    server {
      location / {
        index index.html;
        try_files $uri $uri/ /index.html =404;
      }
    }
